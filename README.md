# IPI Prospecting Tool

Static web app for prospecting and scoring target partners against the IPI Ideal Partner Profile.

## Local run
Open with a local web server, for example:

```bash
python -m http.server 8000
